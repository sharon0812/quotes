import { FavourlightDirective } from './favourlight.directive';

describe('FavourlightDirective', () => {
  it('should create an instance', () => {
    const directive = new FavourlightDirective();
    expect(directive).toBeTruthy();
  });
});
